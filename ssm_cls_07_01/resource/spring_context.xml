<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
 xmlns:context="http://www.springframework.org/schema/context"
 xmlns:aop="http://www.springframework.org/schema/aop"
 xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context.xsd
	http://www.springframework.org/schema/aop 
	http://www.springframework.org/schema/aop/spring-aop.xsd">
	<context:component-scan base-package="com.bawei"></context:component-scan>
	<!-- 配置切面类(增强代码的类) -->
	<bean id="logAop" class="com.bawei.conf.LogAop"></bean>
	<!-- 配置aop时 必须先引入aop的根标签 其它aop的配置必须放在根标签内部-->
	<aop:config>
	    <!-- aop:aspect 称为切面标签 配置切面类的信息 ref="注册切面类的id" -->
		<aop:aspect ref="logAop">
			<!-- 切入点配置  <aop:pointcut expression="切入点表达式" id="切入点id"/>-->
			<!-- 切入点表达式写法:execution([方法的访问修饰符] 返回值类型 包名.类名.方法名(参数)) -->
			<aop:pointcut expression="execution(* com.bawei.service.impl.MeetingServiceImpl.*(..))" id="pointCut"/>
			<!-- 配置增强 -->
			<!-- 后置增强  <aop:after method="指定增强的方法名" pointcut-ref="指定切入点id"/>-->
			<aop:after method="doAfter" pointcut-ref="pointCut"/>
			<!-- 前置通知 -->
			<aop:before method="doBefore" pointcut-ref="pointCut"/>
			<!-- 环绕通知 -->
			<aop:around method="doAround" pointcut-ref="pointCut"/>
			<!-- 异常通知 -->
			<aop:after-throwing method="doThrowable" pointcut-ref="pointCut"/>
			<!-- 最终通知 returning="方法名参数 获取返回值"-->
			<aop:after-returning method="doAfterReturn" pointcut-ref="pointCut" returning="retrunMessage"/>
		</aop:aspect>
	</aop:config>
</beans>









